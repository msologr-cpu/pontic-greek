# Понтийская Греческая Раскладка — Инструкция для macOS

> **Версия:** 1.1  
> **Платформа:** macOS (Ventura 13+ / Sonoma 14+ / Sequoia 15+)  
> **Файл раскладки:** `pnt-macos.keylayout`

---

## Что это такое

Это клавиатурная раскладка для понтийского греческого языка на macOS.  
Она **идентична стандартной греческой раскладке** — за исключением трёх «мёртвых» клавиш:

| Клавиша | Мёртвый символ | Назначение |
|---------|---------------|------------|
| `'` (апостроф) | ˇ (гачек/háček) | ζ̌ χ̌ σ̌ ς̌ κ̌ ξ̌ ψ̌ |
| `/` (слэш) | ̤ (две точки снизу) | α̤ ο̤ |
| `Shift` + `,` | ˘ (бреве) | γ̆ |

Вы можете использовать её вместо обычной греческой. Одна наклейка на ноутбук — и всё.

---

## Установка

### Шаг 1 — Скопируйте файл раскладки

**Вариант А — автоматически (рекомендуется):**

Откройте Терминал (Terminal.app) и выполните:

```bash
cd /путь/к/папке/pontic
bash install_macos.sh
```

Скрипт сам скопирует файл в нужное место и откроет папку в Finder.

**Вариант Б — вручную:**

1. Откройте Finder
2. Нажмите `Cmd + Shift + G` (Перейти к папке)
3. Введите: `~/Library/Keyboard Layouts/`
4. Скопируйте файл `pnt-macos.keylayout` в эту папку

---

### Шаг 2 — Добавьте раскладку в систему

1. Откройте **Системные настройки** (`System Settings`)
2. Перейдите в **Клавиатура** (`Keyboard`)
3. Нажмите **Источники ввода** → **Редактировать...** (`Input Sources` → `Edit...`)
4. Нажмите кнопку **`+`** (внизу слева)
5. В левой колонке выберите **«Другой»** (`Other`)
6. В списке найдите **Pontic Greek**
7. Нажмите **Добавить** (`Add`)

> **Важно:** Если раскладка не появляется в списке — закройте Системные настройки и откройте снова, или перезагрузите Mac.

---

### Шаг 3 — Переключение раскладок

| Способ | Действие |
|--------|----------|
| Клавиша Fn (🌐 глобус) | Переключить язык |
| `Cmd + Space` | Переключить язык (если настроено) |
| Мышь | Клик на флаге/значке в строке меню (правый верхний угол) |

---

## Как набирать понтийские символы

### 🔸 Гачек ˇ (клавиша `'`, физически — апостроф)

**Принцип: нажмите `'`, отпустите, затем нажмите нужную букву.**

| Нажатие | Результат | Описание |
|---------|-----------|----------|
| `'` → `ζ` | **ζ̌** | зета с гачеком |
| `'` → `χ` | **χ̌** | хи с гачеком |
| `'` → `σ` | **σ̌** | сигма с гачеком |
| `'` → `ς` | **ς̌** | конечная сигма с гачеком |
| `'` → `κ` | **κ̌** | каппа с гачеком |
| `'` → `ξ` | **ξ̌** | кси с гачеком |
| `'` → `ψ` | **ψ̌** | пси с гачеком |

**Заглавные:** `'` → `Shift+ζ` = **Ζ̌** (и так далее для всех букв).

> Если после `'` нажать **пробел** — выводится сам символ ˇ.  
> Если после `'` нажать **не ту букву** — выводится `'` + набранная буква.

---

### 🔸 Две точки снизу ̤ (клавиша `/`, физически — слэш)

**Принцип: нажмите `/`, отпустите, затем нажмите нужную букву.**

| Нажатие | Результат | Описание |
|---------|-----------|----------|
| `/` → `α` | **α̤** | альфа с двумя точками снизу |
| `/` → `ο` | **ο̤** | омикрон с двумя точками снизу |

**Заглавные:** `/` → `Shift+α` = **Α̤**, `/` → `Shift+ο` = **Ο̤**

**Ударные варианты (тонос + точки):**  
Сначала наберите ударную букву через `;`, затем нажмите `/`:

| Нажатие | Результат | Описание |
|---------|-----------|----------|
| `;` → `α` → потом набрать `/` → `α` | Не работает напрямую, см. ниже | |

> **Для ударных α̤/ο̤ на macOS:** Раскладка macOS обрабатывает тонос и точки как отдельные мёртвые клавиши. Набирайте: `;` → `α` = `ά`, затем вручную добавьте комбинирующий символ если нужно, или используйте символ из таблицы Unicode.

---

### 🔸 Бреве ˘ (Shift + `,`)

**Принцип: нажмите `Shift+,`, отпустите, затем нажмите нужную букву.**

| Нажатие | Результат | Описание |
|---------|-----------|----------|
| `Shift+,` → `γ` | **γ̆** | гамма с бреве |
| `Shift+,` → `Γ` | **Γ̆** | гамма с бреве (верхний регистр) |

---

### 🔸 Тонос ΄ (клавиша `;` — стандартный греческий)

Работает как в обычной греческой раскладке:

| Нажатие | Результат |
|---------|-----------|
| `;` → `α` | **ά** |
| `;` → `ε` | **έ** |
| `;` → `η` | **ή** |
| `;` → `ι` | **ί** |
| `;` → `ο` | **ό** |
| `;` → `υ` | **ύ** |
| `;` → `ω` | **ώ** |

---

### 🔸 Конечная сигма ς

На стандартной греческой раскладке macOS:
- Клавиша **Q** → `ς` (конечная сигма)
- Клавиша **S** → `σ` (обычная сигма)

Используйте **Q** для набора ς на конце слов.

---

## Краткая шпаргалка

```
ГАЧЕК:        '  +  буква       → ζ̌  χ̌  σ̌  ς̌  κ̌  ξ̌  ψ̌
ДВЕ ТОЧКИ:    /  +  буква       → α̤  ο̤
БРЕВЕ:        Shift+,  +  буква → γ̆
ТОНОС:        ;  +  буква       → ά  έ  ή  ί  ό  ύ  ώ  (стандарт)
КОНЕЧНАЯ ς:   клавиша Q
```

---

## Option (Alt) слой

| Клавиша | Символ |
|---------|--------|
| Option + `ε` | € (евро) |
| Option + `α` | … (многоточие) |
| Option + `ι` | " (кавычки) |
| Option + `ο` | „ (нижние кавычки) |
| Option + `,` | « (ёлочки левые) |
| Option + `.` | » (ёлочки правые) |
| Option + Space | неразрывный пробел |

---

## Если что-то не работает

**Раскладка не появляется в списке при добавлении:**
- Проверьте, что файл лежит в `~/Library/Keyboard Layouts/` (не в системной `/Library/`)
- Закройте Системные настройки и откройте снова
- В крайнем случае — перезагрузите Mac

**Символы с гачеком отображаются неправильно (квадратики, точки):**
- Это зависит от шрифта. Символы Unicode правильные — просто некоторые шрифты плохо рендерят комбинируемые диакритики
- Рекомендуемые шрифты: **Noto Serif**, **Linux Libertine**, **Gentium Plus**

**Клавиша `/` или `'` не работает как мёртвая:**
- Убедитесь, что выбрана именно раскладка **Pontic Greek**, а не обычная греческая
- Проверьте в меню-баре (правый верхний угол) — должна быть выбрана Pontic Greek

**Как удалить раскладку:**
1. Системные настройки → Клавиатура → Источники ввода → выберите Pontic Greek → нажмите `−`
2. Удалите файл `~/Library/Keyboard Layouts/pnt-macos.keylayout`

---

*Версия 1.1 · Июнь 2026*
