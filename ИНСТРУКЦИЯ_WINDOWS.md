# Понтийская Греческая Раскладка — Windows (Портативный EXE-тест)

Это **полностью портативная версия** раскладки для тестирования. Устанавливать ничего не нужно.

Она сделана так, чтобы сохранить **идеально одинаковый опыт** с macOS (когда вы нажимаете мертвую клавишу, а затем букву).

---

## 🚀 Как запустить за 10 секунд

1. **Скачайте архив** `PonticKeyboard_Windows_Test.zip` из репозитория.
2. **Распакуйте** его в любую папку.
3. Убедитесь, что в Windows у вас выбрана стандартная **Греческая** раскладка клавиатуры (переключитесь на нее через `Alt + Shift` или `Win + Пробел`).
4. **Запустите `PonticKeyboard.exe`**.

Всё! В правом нижнем углу экрана (возле часов) появится зелёная иконка "H" — это значит, что мертвые клавиши активированы.

---

## ⌨️ Как тестировать (Шпаргалка)

Переключите язык Windows на **Греческий** и пробуйте:

### 🔸 Гачек `ˇ` (клавиша `'`, физически — апостроф)
Нажмите клавишу апострофа `'`, затем нужную букву:
*   `'` → `ζ` = **ζ̌**
*   `'` → `χ` = **χ̌**
*   `'` → `σ` = **σ̌**
*   `'` → `κ` = **κ̌**
*   `'` → `ξ` = **ξ̌**
*   `'` → `ψ` = **ψ̌**
*(Работает также с заглавными, если зажать Shift: `'` → `Z` = `Ζ̌`)*

### 🔸 Две точки снизу `̤` (клавиша `/`, физически — слэш)
Нажмите `/`, затем нужную букву:
*   `/` → `α` = **α̤**
*   `/` → `ο` = **ο̤**
*(Работает с заглавными Α̤ и Ο̤)*

### 🔸 Бреве `˘` (Shift + `,`)
*   `Shift+,` → `γ` = **γ̆**
*(Работает с заглавной Γ̆)*

### 🔸 Тонос `΄` (клавиша `;` — стандартный греческий)
Работает стандартно благодаря греческой раскладке:
*   `;` → `α` = **ά**
*   `;` → `ο` = **ό**

---

## ❓ Вопросы и ответы

**Нужно ли скачивать или устанавливать какие-то программы?**
Нет, `PonticKeyboard.exe` — это портативная версия AutoHotkey. Ничего устанавливать не нужно.

**Как отключить раскладку?**
Кликните правой кнопкой мыши по зеленой иконке H в трее (внизу экрана) и нажмите **Exit**.

**Она будет работать всегда?**
Пока `PonticKeyboard.exe` запущен — да. Если вы перезагрузите компьютер, вам нужно будет снова кликнуть по нему дважды. Позже мы можем добавить его в автозагрузку.

**Что делать, если антивирус или Windows SmartScreen ругается?**
Поскольку этот EXE-файл новый и скачан из интернета (мы не подписывали его корпоративным сертификатом), Windows может выдать синее окно "Система Windows защитила ваш компьютер".
Нажмите **Подробнее (More info)** → **Выполнить в любом случае (Run anyway)**.
