# Понтийская Греческая Раскладка — Инструкция для Windows

> **Версия:** 1.2  
> **Платформа:** Windows 10 / 11  
> **Файлы:** `PonticKeyboard.ahk` (скрипт) или `PonticKeyboard.exe` (портативный)

---

## Что это такое

Это скрипт AutoHotkey, который добавляет понтийскую «мёртвую клавишу» поверх стандартной греческой раскладки Windows. Все понтийские диакритики собраны на **одной клавише** `/` (слэш) — скрипт автоматически определяет нужный значок по букве:

| Буква | Результат | Диакритик |
|-------|-----------|-----------|
| ζ χ σ ς κ ξ ψ | ζ̌ χ̌ σ̌ ς̌ κ̌ ξ̌ ψ̌ | гачек (ˇ) |
| γ | γ̆ | бреве (˘) |
| α ο | α̤ ο̤ | две точки снизу (̤) |
| ά ό | ά̤ ό̤ | ударная + две точки снизу |

---

## Установка и запуск

### Вариант А — Портативный EXE (без установки)

Это самый простой способ. Устанавливать ничего не нужно.

1. **Скачайте** файл `PonticKeyboard.exe` из репозитория
2. **Переключите** раскладку Windows на **Греческую** (`Alt+Shift` или `Win+Пробел`)
3. **Запустите** `PonticKeyboard.exe` двойным кликом
4. В правом нижнем углу экрана (возле часов) появится зелёная иконка — это значит, что мёртвые клавиши активированы

> **Если Windows SmartScreen ругается** (синее окно «Windows защитила ваш компьютер»):  
> Нажмите **Подробнее** (`More info`) → **Выполнить в любом случае** (`Run anyway`).  
> Это нормально для непонятных Windows программ без цифровой подписи.

### Вариант Б — С установкой AutoHotkey

Если вы хотите редактировать скрипт или AutoHotkey уже установлен:

1. Установите **AutoHotkey v2** с [autohotkey.com](https://www.autohotkey.com/)
2. Скачайте файл `PonticKeyboard.ahk` (или `pontic_windows.ahk`)
3. Переключите раскладку Windows на **Греческую**
4. Запустите `.ahk` файл двойным кликом

---

### Как отключить

- Кликните **правой кнопкой мыши** по зелёной иконке в трее (внизу экрана)
- Нажмите **Exit** (`Выход`)

### Автозагрузка (чтобы не запускать каждый раз)

1. Нажмите `Win+R`, введите `shell:startup` → Enter
2. Скопируйте (или создайте ярлык) `PonticKeyboard.exe` в открывшуюся папку
3. Теперь скрипт будет запускаться автоматически при входе в Windows

---

## Как набирать понтийские символы

### 🔸 Понтийская клавиша `/` (единая мёртвая клавиша)

**Принцип: нажмите `/`, отпустите, затем нажмите нужную букву.** Скрипт сам определит, какой диакритик нужен.

Скрипт ждёт следующий символ в течение 2 секунд. Если ничего не нажать — выводится обычный слэш `/`.

#### Гачек ˇ

| Нажатие | Результат | Описание |
|---------|-----------|----------|
| `/` → `ζ` | **ζ̌** | зета с гачеком |
| `/` → `χ` | **χ̌** | хи с гачеком |
| `/` → `σ` | **σ̌** | сигма с гачеком |
| `/` → `ς` | **ς̌** | конечная сигма с гачеком |
| `/` → `κ` | **κ̌** | каппа с гачеком |
| `/` → `ξ` | **ξ̌** | кси с гачеком |
| `/` → `ψ` | **ψ̌** | пси с гачеком |

**Заглавные:** зажмите `Shift` перед нажатием буквы:  
`/` → `Shift+ζ` = **Ζ̌** (и так далее для всех букв)

#### Бреве ˘

| Нажатие | Результат | Описание |
|---------|-----------|----------|
| `/` → `γ` | **γ̆** | гамма с бреве |
| `/` → `Γ` | **Γ̆** | гамма с бреве (заглавная) |

#### Две точки снизу ̤

| Нажатие | Результат | Описание |
|---------|-----------|----------|
| `/` → `α` | **α̤** | альфа с двумя точками снизу |
| `/` → `ο` | **ο̤** | омикрон с двумя точками снизу |
| `/` → `ά` | **ά̤** | ударная альфа с точками снизу |
| `/` → `ό` | **ό̤** | ударный омикрон с точками снизу |

**Заглавные:** `/` → `Shift+α` = **Α̤**, `/` → `Shift+ο` = **Ο̤**

**Ударные варианты (тонос + точки снизу):**  
Для набора ά̤ или ό̤:

| Последовательность | Результат |
|--------------------|-----------|
| `;` → `α` = `ά`, затем `/` → `ά` | **ά̤** |
| `;` → `ο` = `ό`, затем `/` → `ό` | **ό̤** |

> После набора ά̤ можно сразу продолжать печатать — нажимать пробел не нужно.

#### Другие комбинации

> `/` + пробел = `/` (обычный слэш)  
> `/` + другая буква = `/` + эта буква (обычный ввод)  
> `Shift` + `/` + таймаут = `?` (вопросительный знак)

---

### 🔸 Конечная сигма с гачеком ς̌

На греческой раскладке Windows клавиша **W** = `ς`.  
Поэтому: `/` → `W` = **ς̌**

---

### 🔸 Тонос ΄ (клавиша `;` — стандартный греческий)

Тонос работает **нативно** через стандартную греческую раскладку Windows. Скрипт его не трогает.

| Нажатие | Результат |
|---------|-----------|
| `;` → `α` | **ά** |
| `;` → `ε` | **έ** |
| `;` → `η` | **ή** |
| `;` → `ι` | **ί** |
| `;` → `ο` | **ό** |
| `;` → `υ` | **ύ** |
| `;` → `ω` | **ώ** |

---

### 🔸 Конечная сигма ς

На стандартной греческой раскладке Windows:
- Клавиша **S** → `σ` (обычная сигма, для середины слова)
- Клавиша **W** → `ς` (конечная сигма, для конца слова)

**Используйте клавишу W для набора ς на конце слов.**

> В стандартной греческой раскладке Windows автоматической замены σ→ς нет — это особенность раскладки. Нужно явно нажимать W для конечной формы.

---

## Краткая шпаргалка

```
ПОНТИЙСКАЯ КЛАВИША:  /  +  буква

  / + ζ χ σ ς κ ξ ψ  → гачек:        ζ̌  χ̌  σ̌  ς̌  κ̌  ξ̌  ψ̌
  / + γ               → бреве:        γ̆
  / + α ο             → точки снизу:  α̤  ο̤
  / + ά ό             → ударн.+точки: ά̤  ό̤

ТОНОС:    ;  +  буква       → ά  έ  ή  ί  ό  ύ  ώ  (стандарт)
КОНЕЧНАЯ ς:   клавиша W

Таймаут: 2 секунды. Если ничего не нажать — выводится обычный /
```

---

## Рекомендуемые шрифты

Не все шрифты поддерживают combining diacritical marks с греческими буквами.

| Шрифт | Поддержка |
|-------|-----------|
| **Cambria** | ✅ Полная поддержка |
| **Brill** | ✅ Полная поддержка |
| **Noto Sans** / **Noto Serif** | ✅ Полная поддержка |
| **Gentium Plus** | ✅ Полная поддержка |
| **Calibri** | ⚠️ Не отображает ψ̌ |
| **Times New Roman** | ❌ Не поддерживает combining caron с греческими |

---

## Раскладка клавиатуры (стандартная греческая Windows)

```
Обычный слой:
  ; ς ε ρ τ υ θ ι ο π [ ] \
  α σ δ φ γ η ξ κ λ ΄ '
  ζ χ ψ ω β ν μ , . /→понтийская

Shift:
  : ΅ Ε Ρ Τ Υ Θ Ι Ο Π { } |
  Α Σ Δ Φ Γ Η Ξ Κ Λ ΄ "
  Ζ Χ Ψ Ω Β Ν Μ < > ?
```

> Стрелка `→` показывает, какая клавиша перехвачена скриптом.

---

## Вопросы и ответы

**Нужно ли скачивать или устанавливать какие-то программы?**  
Нет, `PonticKeyboard.exe` — портативная версия. Ничего устанавливать не нужно.

**Exe-файл не запускается / антивирус блокирует?**  
Некоторые антивирусы блокируют AutoHotkey-скрипты. Добавьте файл в исключения антивируса, или используйте вариант Б (установите AutoHotkey v2 и запускайте `.ahk` файл напрямую).

**Как отключить раскладку?**  
Кликните правой кнопкой мыши по иконке в трее → **Exit**.

**Она будет работать всегда?**  
Пока `PonticKeyboard.exe` запущен — да. После перезагрузки нужно запустить снова. Для автозагрузки — см. раздел «Автозагрузка» выше.

**Мёртвая клавиша не сработала, вывелся обычный символ:**  
- Убедитесь, что раскладка Windows переключена на **Греческую**
- Возможно, вы нажали букву слишком поздно (таймаут 2 секунды)

**Символы отображаются как квадратики или точки:**  
Это зависит от шрифта. Используйте **Cambria**, **Brill** или **Noto Serif** — они поддерживают все понтийские символы.

**В старой версии были отдельные клавиши ' и Shift+, — а теперь?**  
В версии 1.2 все понтийские диакритики объединены на одной клавише `/`. Клавиши `'` (апостроф) и `Shift+,` (`<`) теперь работают стандартно, без перехвата.

---

*Версия 1.2 · Июнь 2026*
