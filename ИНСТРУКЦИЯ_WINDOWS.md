# Понтийская Греческая Раскладка — Инструкция для Windows

> **Версия:** 3.5
> **Платформа:** Windows 10 / 11
> **Файлы:** `PonticKeyboard_v3.5.exe` (одна клавиша `/`) или `PonticKeyboard_v3.5_3keys.exe` (три клавиши `,` `.` `/`)

---

## Про номера версий — чтобы больше не путаться

Раньше файлы назывались `PonticKeyboard_v3.exe`, а в инструкции писалось «версия v3.4».
Понять, какая версия внутри, было невозможно. **Это исправлено.**

Теперь:

1. **Номер версии стоит прямо в имени файла**: `PonticKeyboard_v3.5.exe`.
2. **При запуске** в правом нижнем углу появляется уведомление: «Версия 3.5 запущена».
3. **В любой момент** можно нажать **Ctrl + Alt + P** — откроется окно диагностики,
   где написана работающая версия, текущая раскладка и состояние Word.

Если в окне диагностики стоит версия **не 3.5** — значит запущен старый файл.

---

## Главное в версии 3.5: причина проблем найдена

Проблемы с `σ̌` (превращалась в `ς̌`) и с `τ'` (превращалась в `τα'`)
возникали **не из-за Windows и не из-за Office**. Виновата была одна функция
самого Word — **автозамена (AutoCorrect)**:

- Word не считает понтийский значок «гачек» буквой. Поэтому после `σ` + гачек
  он решал, что слово закончилось, и менял `σ` на конечную `ς`.
- Апостроф после `τ` Word подменял по своему греческому списку автозамен.

Раньше мы пытались обойти автозамену хитростью — не помогало.
**Теперь программа просто отключает автозамену в Word сама**, автоматически,
как только Word запущен. Причина устранена, а не замаскирована.

Дополнительно в меню (правая кнопка по значку в трее) есть пункт
**«Починить MS Word (отключить автозамену)»** — на случай, если захочется
применить исправление вручную.

---

## Как установить и пользоваться

1. **Скачайте** нужный файл `.exe`.
2. **Переключите** раскладку Windows на **Греческую** (`Alt + Shift` или `Win + Пробел`).
   Это обязательно — без греческой раскладки программа не вмешивается в набор.
3. **Запустите** `.exe` двойным кликом. Появится уведомление с номером версии.
4. Откройте Word (или блокнот, мессенджер) и печатайте.

> **Если Windows покажет «Защита компьютера»** — нажмите «Подробнее» → «Выполнить в любом случае».
> Это обычное предупреждение для программ без платной цифровой подписи.

---

## ⚠️ Если раньше была запущена старая версия

Обязательно закройте её, иначе две копии будут мешать друг другу:

1. Найдите в правом нижнем углу экрана (возле часов) значок программы.
   Возможно, придётся нажать стрелочку «Показать скрытые значки».
2. Нажмите по значку **правой кнопкой мыши** → **Выход**.
3. Только теперь запускайте новый файл.

Проверить, что работает именно новая версия, можно клавишами **Ctrl + Alt + P**.

---

## Варианты раскладок

### Вариант 1 — `PonticKeyboard_v3.5.exe` (одна клавиша `/`)

Все понтийские значки на одной клавише `/` (слэш):

| Нажатие | Затем буква | Результат | Значок |
|---|---|---|---|
| `/` | ζ χ σ ς κ ξ ψ | ζ̌ χ̌ σ̌ ς̌ κ̌ ξ̌ ψ̌ | гачек (ˇ) |
| `/` | γ | γ̆ | бреве (˘) |
| `/` | α ο | α̤ ο̤ | две точки снизу |
| `/` | ά ό | ά̤ ό̤ | тонос + две точки снизу |

Обычный слэш печатается как всегда: `/` + пробел, либо `/` перед любой другой буквой.

### Вариант 2 — `PonticKeyboard_v3.5_3keys.exe` (три клавиши)

Три соседние клавиши в нижнем ряду:

- `,` (запятая) → **гачек**: `,` + `ζ` = `ζ̌`, `,` + `σ` = `σ̌`, `,` + `ς` = `ς̌`
- `.` (точка) → **две точки снизу**: `.` + `α` = `α̤`, `.` + `ο` = `ο̤`
- `/` (слэш) → **бреве**: `/` + `γ` = `γ̆`

Обычные `,` `.` `/` печатаются как всегда, если следом идёт не понтийская буква.

### Про две сигмы

- Клавиша **`S`** → `σ̌` (сигма в середине слова)
- Клавиша **`W`** → `ς̌` (конечная сигма)

Так же, как в обычной греческой раскладке Windows.

---

## Если что-то не работает

1. Нажмите **Ctrl + Alt + P** — откроется окно диагностики.
2. Проверьте три строки:
   - **Версия скрипта** — должна быть 3.5;
   - **Раскладка клавиатуры** — должна быть «ГРЕЧЕСКАЯ — верно»;
   - **MS Word** — должно быть «автозамена ОТКЛЮЧЕНА — всё верно».
3. Если какая-то строка не такая — **сделайте снимок экрана этого окна и пришлите**.
   По нему сразу видно, в чём дело.

Снимок экрана в Windows: клавиша **PrtScn**, либо **Win + Shift + S** (выделить область),
затем вставить в письмо клавишами **Ctrl + V**.
