# Понтийская Греческая Раскладка — Инструкция для Windows

> **Версия:** 3.4 (Июль 2026)  
> **Платформа:** Windows 10 / 11  
> **Файлы:** `PonticKeyboard_v3.ahk` (1 слэш `/`) или `PonticKeyboard_v3_3keys.ahk` (3 клавиши `,` `.` `/`)

---

## 🆕 Что нового в v3.4

1. **Решена проблема с автозаменой сигмы (`σ̌`) в MS Word**:
   - Отправка медиальной сигмы `σ̌` теперь происходит атомарно через буфер обмена (Clipboard).
   - Это полностью обходит встроенную поклавишную автозамену MS Word (`σ` $\rightarrow$ `ς` на конце незавершённого слова), из-за которой между гласными появлялась `ς̌` вместо `σ̌`.
2. **Конечная сигма с гачеком (`ς̌`)**:
   - Нажатие `S` $\rightarrow$ `σ̌` (медиальная сигма)
   - Нажатие `W` $\rightarrow$ `ς̌` (конечная сигма)
3. **Защита от бага MS Word с апострофом (`τ’`)**:
   - Автоматическая коррекция `τ'` $\rightarrow$ `τ’` (U+2019) предотвращает искажения в Word.
4. **Совместимость с русской раскладкой**:
   - Перехваты клавиш работают только при активной греческой раскладке.

---

## ⚠️ ВАЖНО: Если у вас была запущена старая версия!

1. Найдите в правом нижнем углу экрана (возле часов / в трее Windows) иконку скрипта (зелёный значок H или слэш).
2. Нажмите по ней **правой кнопкой мыши** → **Exit** (Выход).
3. Запустите новый файл `PonticKeyboard_v3.exe` или `PonticKeyboard_v3_3keys.exe`.

---

## Варианты раскладок

### Вариант 1 — `PonticKeyboard_v3.exe` (Единая мёртвая клавиша `/`)

Все понтийские значки находятся на одной клавише `/` (слэш):

| Нажатие | Буква | Результат | Символ |
|---|---|---|---|
| `/` | ζ χ σ ς κ ξ ψ | ζ̌ χ̌ σ̌ ς̌ κ̌ ξ̌ ψ̌ | гачек (ˇ) |
| `/` | γ | γ̆ | бреве (˘) |
| `/` | α ο | α̤ ο̤ | две точки снизу (̤) |
| `/` | ά ό | ά̤ ό̤ | тонос + две точки снизу |

### Вариант 2 — `PonticKeyboard_v3_3keys.exe` (3 раздельные клавиши)

Три клавиши в нижнем ряду:
- `,` (запятая) → гачек (ˇ): `,` + `ζ` = `ζ̌`, `,` + `σ` = `σ̌`, `,` + `ς` = `ς̌`
- `.` (точка) → две точки снизу (̤): `.` + `α` = `α̤`, `.` + `ο` = `ο̤`
- `/` (слэш) → бреве (˘): `/` + `γ` = `γ̆`

---

## Инструкция по использованию

1. **Скачайте** выбранный файл `.exe`.
2. **Переключите** раскладку Windows на **Греческую** (`Alt+Shift` или `Win+Пробел`).
3. **Запустите** `.exe` двойным кликом.
4. Выберите место для ввода текста (Word, блокнот, мессенджер) и печатайте!
