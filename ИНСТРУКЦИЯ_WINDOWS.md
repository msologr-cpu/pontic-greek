# Понтийская Греческая Раскладка — Windows (Тестовая версия)

Для тестирования на Windows мы используем **AutoHotkey** — крошечную утилиту, которая добавит нужные нам мертвые клавиши поверх вашей стандартной греческой раскладки. 

Это позволяет сделать процесс набора текста **1 в 1 таким же, как на macOS** (где вы нажимаете мертвую клавишу, а затем букву).

---

## 🚀 Как установить и запустить (за 2 минуты)

### Шаг 1. Включите стандартную Греческую раскладку в Windows
Если у вас её еще нет:
1. Откройте **Параметры Windows** (Win + I) → **Время и язык** → **Язык и регион**.
2. Нажмите **Добавить язык** и выберите **Греческий**.
3. Убедитесь, что язык установлен (вы можете переключаться на него через `Alt + Shift` или `Win + Пробел`).

### Шаг 2. Установите AutoHotkey
AutoHotkey — это безопасная и самая популярная в мире программа для переназначения клавиш.
1. Скачайте **AutoHotkey v2** с официального сайта: [autohotkey.com/download/ahk-v2.exe](https://autohotkey.com/download/ahk-v2.exe)
2. Установите программу (достаточно нажать "Install"). Никаких сложных настроек не требуется.

### Шаг 3. Запустите Понтийскую раскладку
1. Скачайте файл **`pontic_windows.ahk`** из этого репозитория.
2. Просто **дважды кликните** по этому файлу.
3. В системном трее (внизу справа возле часов) появится зеленая иконка с буквой **H** или иконка клавиатуры — скрипт работает!

---

## ⌨️ Как тестировать (Шпаргалка)

Переключите язык Windows на **Греческий** и пробуйте:

### 🔸 Гачек `ˇ` (клавиша `'`, физически — апостроф)
Нажмите клавишу апострофа `'`, затем нужную букву:
*   `'` → `ζ` = **ζ̌**
*   `'` → `χ` = **χ̌**
*   `'` → `σ` = **σ̌**
*   `'` → `κ` = **κ̌**
*   `'` → `ξ` = **ξ̌**
*   `'` → `ψ` = **ψ̌**
*(Работает также с заглавными, если зажать Shift: `'` → `Z` = `Ζ̌`)*

### 🔸 Две точки снизу `̤` (клавиша `/`, физически — слэш)
Нажмите `/`, затем нужную букву:
*   `/` → `α` = **α̤**
*   `/` → `ο` = **ο̤**
*(Работает с заглавными Α̤ и Ο̤)*

### 🔸 Бреве `˘` (Shift + `,`)
*   `Shift+,` → `γ` = **γ̆**
*(Работает с заглавной Γ̆)*

### 🔸 Тонос `΄` (клавиша `;` — стандартный греческий)
Работает стандартно благодаря греческой раскладке:
*   `;` → `α` = **ά**
*   `;` → `ο` = **ό**

---

## ❓ Вопросы и ответы

**Как отключить раскладку?**
Кликните правой кнопкой мыши по зеленой иконке H (или клавиатуры) в трее внизу экрана и нажмите **Exit**.

**Она будет работать всегда?**
Пока файл `pontic_windows.ahk` запущен — да. Если вы перезагрузите компьютер, вам нужно будет снова кликнуть по нему дважды. Если тесты пройдут успешно, этот файл можно добавить в автозагрузку Windows.

**Почему не обычный установщик .exe?**
Это тестовая сборка, чтобы мы могли точно проверить логику мертвых клавиш. Если всё идеально подойдет, мы скомпилируем этот скрипт в один самостоятельный файл `.exe` или соберем через Keyman, который не будет требовать установки AutoHotkey.
