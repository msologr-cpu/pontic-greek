# Понтийская клавиатура для телефона (Android / iPhone)

> **Версия:** 4.0
> **Файл:** `pontic_greek.kmp`
> **Приложение:** Keyman (бесплатное, из Google Play / App Store)

---

## Где скачать

**https://github.com/msologr-cpu/pontic-greek/releases/latest**

На этой странице в списке **Assets** найдите файл **`pontic_greek.kmp`**.
Ссылка постоянная — она всегда ведёт на самую свежую версию.

---

## Про номера версий

Раньше было две отдельные нумерации: программа для Windows дошла до 3.4,
а мобильная клавиатура оставалась на 3.0. Это только сбивало с толку.

**Теперь номер версии общий для всех устройств** — и Windows, и Android,
и iPhone. Если номера совпадают, значит у вас везде одно и то же.

---

## Что нового в версии 4.0

**Гачек больше не налезает на буквы** — в клавиатуру встроен собственный
шрифт **Pontic Sans**. Подробности ниже, в пункте 3.

## Что было исправлено в версии 3.5

По отзыву носителя языка исправлены три проблемы:

### 1. Не вводились ЗАГЛАВНЫЕ буквы

**Причина найдена.** В раскладке стояла настройка «после заглавной буквы сразу
вернуться к строчным». Она срабатывала слишком рано — раньше, чем буква успевала
вставиться. Буква подсвечивалась синим, но в текст не попадала.

**Что сделано:** эта настройка убрана со всех клавиш. Теперь Shift работает
как обычно: нажали Shift → напечатали заглавную → нажали Shift ещё раз для
возврата к строчным.

### 2. Не вводились знаки препинания

**Причина.** Запятая, апостроф, скобки и прочее были спрятаны в «долгое нажатие»
под точкой. Долгое нажатие срабатывало ненадёжно.

**Что сделано:** запятая и апостроф теперь **отдельные видимые клавиши**
в нижнем ряду, рядом с пробелом. Нижний ряд теперь такой:

```
[123]  [ , ]  [   пробел   ]  [ ' ]  [ . ]  [Enter]
```

А под ними, если нажать и подержать:

| Клавиша | Долгое нажатие даёт |
|---|---|
| `,` | `;` `:` `(` `)` `-` `/` |
| `'` | `’` `"` `«` `»` |
| `.` | `?` `!` `…` |

Всё это работает и в обычном режиме, и с нажатым Shift.

### 3. Гачек над буквами сидел слишком низко — ИСПРАВЛЕНО в 4.0

**Причина найдена, и Ваше наблюдение оказалось ключом к разгадке.**

Вы заметили, что гачек стоит правильно только над `Ζ` и `Χ`. Это не случайность.
В шрифте телефона положение значка над каждой буквой задаётся отдельной
«точкой привязки». Оказалось, что в стандартном шрифте Noto Sans такие точки
прописаны только для части греческих букв:

| | Буквы |
|---|---|
| Точка **была** | Α **Χ** Κ Ο **Ζ** α χ κ ο |
| Точки **не было** | ξ ζ ψ σ ς γ **Σ Ξ Ψ Γ** |

Ровно те буквы, что Вы назвали правильными, оказались в первом списке,
а все проблемные — во втором. Без точки привязки значок ставится куда придётся
и налезает на букву.

**Что сделано:** мы собрали собственный шрифт **Pontic Sans** на основе
Noto Sans (его лицензия это разрешает) и дописали в него 10 недостающих точек
привязки. Шрифт встроен прямо в клавиатуру — ставить отдельно ничего не нужно.

Проверено на всех сочетаниях: 7 исправлено, а те, что работали раньше
(`Ζ̌`, `Χ̌`, `Κ̌`), остались как были — ничего не сломалось.

**Что важно понимать:** новый шрифт работает **на клавишах самой клавиатуры**.
Текст внутри WhatsApp или заметок рисуется шрифтом того приложения — на это
клавиатура повлиять не может в принципе. Но сами буквы записываются правильно:
откройте этот текст на компьютере — там всё будет верно.

---

## Как установить

1. Установите приложение **Keyman** из Google Play (Android) или App Store (iPhone).
2. Скачайте файл **`pontic_greek.kmp`**.
3. Откройте скачанный файл — телефон предложит открыть его через Keyman.
4. Keyman спросит, установить ли клавиатуру «Pontic Greek» — согласитесь.
5. Разрешите Keyman работать как клавиатура (телефон проведёт по шагам).

**Если раньше стояла старая версия** — сначала удалите её в Keyman:
меню Keyman → «Клавиатуры» → нажать на «Pontic Greek» → «Удалить».
Затем установите новый файл.

---

## ❗ Важно: как НЕ испортить клавиатуры других языков

Когда Keyman включается как клавиатура телефона, он **заменяет собой клавиатуру
для всех языков сразу** — английского, русского, немецкого, новогреческого.
Именно поэтому у носителя языка «к каждой букве прибавился дополнительный
символ» — это не поломка, это внешний вид клавиатур самого Keyman.

**Как вернуть привычные клавиатуры для остальных языков:**

1. Откройте **Настройки** телефона.
2. Найдите раздел **«Система» → «Язык и ввод» → «Экранная клавиатура»**
   (на разных телефонах называется чуть по-разному).
3. Убедитесь, что там **включены обе** клавиатуры: и **Gboard** (обычная), и **Keyman**.
4. Теперь при наборе текста в углу клавиатуры появится значок «🌐» (глобус)
   или значок клавиатуры — **нажатием на него вы переключаетесь**
   между обычной клавиатурой и понтийской.

Итог: для русского, английского, немецкого пользуйтесь Gboard,
а для понтийского переключайтесь на Keyman. Обе живут одновременно.

---

## Как печатать понтийские буквы

Есть два способа — работает любой, как удобнее.

### Способ 1: долгое нажатие на букву

Нажмите и **подержите** букву — сверху появятся варианты с понтийскими значками.
Не отпуская палец, сдвиньте его на нужный вариант и отпустите там.

| Подержать | Появится |
|---|---|
| `ζ` | `ζ̌` |
| `σ` | `σ̌` `ς` `ς̌` |
| `χ` | `χ̌` |
| `κ` | `κ̌` |
| `ξ` | `ξ̌` |
| `ψ` | `ψ̌` |
| `γ` | `γ̆` |
| `α` | `ά` `α̤` `ά̤` |
| `ο` | `ό` `ο̤` `ό̤` |

### Способ 2: сначала буква, потом значок

Напечатайте обычную букву, затем подержите клавишу `΄` (правая верхняя)
и выберите значок `ˇ`. Значок добавится к уже напечатанной букве.

### Заглавные буквы

Нажмите **Shift** (стрелка вверх слева), затем нужную букву.
Для понтийских заглавных — Shift, затем **подержите** букву.
